---
import BaseLayout from "../layouts/BaseLayout.astro";
import { Code } from "astro:components";
import { SITE_TITLE, SITE_DESCRIPTION } from "../consts";

const code = `describe("R/Shiny Developer", {
  it("should write efficient backend", { })
  it("should write aesthetic frontend", { })
  it("should write automated tests", { })
})
`;
---

<!doctype html>
<html lang="en">
  <BaseLayout title={SITE_TITLE} description={SITE_DESCRIPTION}>
    <main class="max-w-[65ch] px-2 mx-auto">
      <h1 class="lowercase font-code text-5xl mt-6">jakub::sobolewski</h1>
      <div class="mt-6 text-xl sm:text-md">
        <Code code={code} , lang="r" theme="github-light" />
      </div>
    </main>
  </BaseLayout>
</html>
