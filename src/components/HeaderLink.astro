---
import AnimatedHeaderLink from "./AnimatedHeaderLink.astro";

const { href, class: className } = Astro.props;
---

<li class:list={["flex items-baseline py-4", className]}>
  <AnimatedHeaderLink href={href} }>
    <slot />
  </AnimatedHeaderLink>
</li>
