---
import BaseHead from "../components/layout/BaseHead.astro";
import Footer from "../components/layout/Footer.astro";
import HeaderDefault from "../components/layout/HeaderDefault.astro";
---

<html lang="en">
  <head>
    <style is:global>
      html {
        scroll-behavior: smooth;
      }
    </style>
    <script is:inline>
      // Dark mode initialization to prevent flash
      (function () {
        const theme =
          localStorage.getItem("theme") ||
          (window.matchMedia("(prefers-color-scheme: dark)").matches
            ? "dark"
            : "light");
        if (theme === "dark") {
          document.documentElement.classList.add("dark");
        }
      })();
    </script>
    <BaseHead
      {...{
        title: "Jakub Sobolewski — R & Shiny, Test-Driven Software Development",
        description:
          "Learn how to effectively test in R and use tests to drive your development process.",
        ...Astro.props,
      }}
    />
    <HeaderDefault />
    <slot name="head" />
  </head>
  <body class="">
    <slot />
    <script src="../utils/observer.js"></script>
    <script src="../scripts/global-search.js"></script>
    <Footer />
  </body>
</html>
